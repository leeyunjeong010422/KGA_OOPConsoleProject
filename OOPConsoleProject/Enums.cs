namespace OOPConsoleProject
{
    public enum SceneType { Title, GameIntroduction, Select, Map, Map1, Map2, Map3, Battle, Inventory, Shop, GameOver, Clear, Return, Size }

    public enum Job { 한지우 = 1, 최이슬, 웅이, 봄이 }

    public enum JiuPoketMonster { 피카츄 = 1, 꼬부기, 이상해씨 } 

    public enum IseulPoketMonster { 고라파덕 = 1, 갸라도스, 별가사리 } 

    public enum UngPoketMonster { 꼬마돌 = 1, 크로뱃, 롱스톤 }

    public enum BomPoketMonster { 뷰티플라이 = 1, 잠만보, 이브이 }

    public enum VillainType { Rocket, Magma, Aqua, Galactic }
}
